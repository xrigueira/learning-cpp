#include <iostream>

using namespace std;

int main() {

    int x = 3, y = 50;
    cout << (x > y) << endl; // Returns 1 if it is True. Basically 1 is True and 0 is False

    cout << (x < 4 && x < 5) << endl;

    return 0;
}